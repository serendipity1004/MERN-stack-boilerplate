/**
 * Root
 * */
import RootRoute from './root/root';

/**
 * Exports
 * */

const mainRoutes = [
    {
        path: '/',
        component:RootRoute,
        wrapper:''
    }
];

export{
    mainRoutes
}